#include "transliteration.h"
#include <cassert>

int main()
{
    assert(Transliteration::toIAST("example") == "example");   // Replace with actual test cases
    assert(Transliteration::fromIAST("example") == "example"); // Replace with actual test cases
    return 0;
}